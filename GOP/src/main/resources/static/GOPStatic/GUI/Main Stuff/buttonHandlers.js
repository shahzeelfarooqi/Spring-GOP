/* Called when the 'Submit Comment' button is pressed
 * This function may not actually be required as the button is part of a form, please
 * check this first before implementing this function.
 */
function submitComment(){
    var commentText = document.getElementById("commentBox");
    var commentUser = document.getElementById("commentUserBox");
}